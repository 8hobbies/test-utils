{
  "name": "@8hobbies/test-utils",
  "version": "1.0.0",
  "description": "Test utilities.",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "files": [
    "/dist",
    "!/dist/**.test.d.ts",
    "!/dist/**.test.js",
    "!/dist/jest.config.d.ts",
    "!/dist/jest.config.js"
  ],
  "scripts": {
    "clean": "rimraf dist/",
    "compile": "tsc --project tsconfig.json && tsc --project tsconfig.test.json",
    "coverage": "vitest run --coverage --run",
    "doc": "typedoc",
    "format": "eslint --fix *.ts *.mjs && prettier . --write",
    "lint": "eslint *.ts *.mjs && prettier . --check",
    "prepack": "npm run clean && npm run compile",
    "test": "npm run compile && npm run coverage",
    "test-all": "npm run compile && npm run lint && npm run test && npm run doc"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/8hobbies/test-utils"
  },
  "keywords": [
    "utils"
  ],
  "author": "Hong Xu",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://gitlab.com/8hobbies/test-utils/issues"
  },
  "homepage": "https://test-utils.8hobbies.com",
  "devDependencies": {
    "@8hobbies/eslint-conf-baseline": "^2.1.0",
    "@8hobbies/typedoc-plugin-404": "^2.0.0",
    "@babel/preset-env": "^7.24.3",
    "@babel/preset-typescript": "^7.24.1",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.30",
    "@vitest/coverage-v8": "^1.4.0",
    "globals": "^14.0.0",
    "prettier": "^3.2.5",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typedoc": "^0.25.12",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "vitest": "^1.4.0"
  }
}
